services:
  # PostgreSQL 17
  db:
    image: postgres:17
    environment:
      - POSTGRES_DB=api_node
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Senh4_123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: